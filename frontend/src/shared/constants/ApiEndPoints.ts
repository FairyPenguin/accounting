const APIEndPoints = {
    // projects: "projects",
    // projects: "projects",
    // projects: "projects",
    // projects: "projects",
    // projects: "projects",
    // projects: "projects",
    // projects: "projects",
    // projects: "projects",
    // projects: "projects",
    // projects: "projects",
    // projects: "projects",
    // projects: "projects",
} as const;

// 3 Kinds

// single by UUID
